services:
  db:
    image: postgres:18-alpine
    container_name: postgres-db
    environment:
      POSTGRES_DB: social
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
    volumes:
     - db-data:/var/lib/postgresql/
    ports:
     - "5432:5432"
volumes:
  db-data:
